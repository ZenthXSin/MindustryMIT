name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Version bump type (patch / minor / major)'
        required: false
        default: patch

permissions:
  contents: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Create local.properties
        run: |
          cat > local.properties <<EOF
          sdk.dir=${{ runner.tool_cache }}/Android/Sdk
          KEYSTORE_PATH=mindustrymit/release.jks
          KEYSTORE_PASSWORD=mmit1234
          KEY_ALIAS=mmit
          KEY_PASSWORD=mmit1234
          EOF

      - name: Resolve next version
        id: ver
        run: |
          BUMP="${{ github.event.inputs.version_bump || 'patch' }}"
          LATEST=$(git tag --sort=-v:refname | head -n1)
          if [ -z "$LATEST" ]; then
            TAG="v0.1.0"
          else
            MAJOR=$(echo "$LATEST" | sed 's/v\([0-9]*\).*/\1/')
            MINOR=$(echo "$LATEST" | sed 's/v[0-9]*\.\([0-9]*\).*/\1/')
            PATCH=$(echo "$LATEST" | sed 's/v[0-9]*\.[0-9]*\.\([0-9]*\)/\1/')
            case "$BUMP" in
              major) MAJOR=$((MAJOR + 1)); MINOR=0; PATCH=0 ;;
              minor) MINOR=$((MINOR + 1)); PATCH=0 ;;
              *)    PATCH=$((PATCH + 1)) ;;
            esac
            TAG="v${MAJOR}.${MINOR}.${PATCH}"
          fi
          VERSION="${TAG#v}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Next release: $TAG"

      - name: Build tool.jar
        run: ./gradlew shadowJar -Pversion=${{ steps.ver.outputs.version }} --no-daemon --stacktrace

      - name: Build release APK
        run: ./gradlew :mindustrymit:assembleRelease --no-daemon --stacktrace

      - name: Stage release artifact
        run: |
          mkdir -p release
          cp build/libs/tool-${{ steps.ver.outputs.version }}.jar "release/tool-${{ steps.ver.outputs.version }}.jar"
          cp src/main/web/index.html "release/web.html"
          cp mindustrymit/build/outputs/apk/release/*.apk "release/mindustrymit-release.apk"

      - name: Create tag and GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.ver.outputs.tag }}
          name: ${{ steps.ver.outputs.tag }}
          generate_release_notes: true
          files: |
            release/tool-${{ steps.ver.outputs.version }}.jar
            release/web.html
            release/mindustrymit-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}